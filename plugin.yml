name: TheEnderchest
main: the\enderchest\Loader
api: 5.0.0
version: 2.0.0

load: POSTWORLD
depend: TheLibrary

permissions:
  enderchest.command:
    default: op
  36.slots.use:
    default: op
  45.slots.use:
    default: op
  54.slots.use:
    default: op
